{
  "name": "ingenium-ctrlaltwin",
  "version": "1.0.0",
  "description": "Privacy-centric multi-login digital identity web application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["digital-identity", "privacy", "healthcare", "multi-login"],
  "author": "CtrlAltWin Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
